# 第 20 章：包和模块详解

> 组织 Rust 代码的基本单元

---

## 20.1 Crate：最小的编译单元

### 什么是 Crate

```
┌─────────────────────────────────────────────────────┐
│              Crate 是什么                            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Crate 是 Rust 最小的编译单元                        │
│                                                     │
│  两种类型：                                          │
│  ┌─────────────────────────────────────────────┐   │
│  │ 库 Crate (Library Crate)                    │   │
│  │  - 不能独立运行，提供可复用的代码            │   │
│  │  - 根文件：src/lib.rs                        │   │
│  └─────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────┐   │
│  │ 二进制 Crate (Binary Crate)                 │   │
│  │  - 可独立运行的程序                          │   │
│  │  - 根文件：src/main.rs                       │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### Package：Crate 的容器

```bash
my_package/                  # 一个 Package
├── Cargo.toml               # Package 的清单文件
├── src/
│   ├── lib.rs               # 库 Crate（可选）
│   └── main.rs              # 二进制 Crate（可选）
└── tests/                   # 集成测试
```

一个 Package 可以包含：
- 至多一个库 Crate
- 多个二进制 Crate
- 但不能只有二进制 Crate 而没有库 Crate

---

## 20.2 模块系统基础

### 为什么需要模块

```rust
// 没有模块：所有代码混在一起
fn process_data() {}
fn save_file() {}
fn send_email() {}
fn validate_user() {}
// ... 1000 行后
fn process_data() {}  // ❌ 命名冲突！

// 有模块：代码组织清晰
mod data {
    pub fn process() {}
}

mod file {
    pub fn save() {}
}

mod email {
    pub fn send() {}
}

mod user {
    pub fn validate() {}
}
```

### 定义模块

```rust
// 方式 1：内联定义（适合小模块）
mod front_of_house {
    pub mod hosting {
        pub fn add_to_waitlist() {
            println!("Added to waitlist");
        }

        pub fn seat_at_table(table_id: usize) -> usize {
            println!("Seating at table {}", table_id);
            table_id
        }
    }

    mod serving {
        fn take_order() {}
        fn serve_order() {}
    }
}

fn main() {
    // 使用绝对路径
    front_of_house::hosting::add_to_waitlist();

    // 使用相对路径
    crate::front_of_house::hosting::add_to_waitlist();
}
```

### 模块树结构

```
crate（根）
└── front_of_house
    ├── hosting
    │   ├── add_to_waitlist
    │   └── seat_at_table
    └── serving
        ├── take_order
        └── serve_order

关键点：
• 模块可以嵌套
• pub 控制可见性
• 默认所有项都是私有的
```

---

## 20.3 路径：绝对路径 vs 相对路径

### 绝对路径

```rust
// 从 crate 根开始
crate::front_of_house::hosting::add_to_waitlist();

// 或者省略 crate（推荐）
front_of_house::hosting::add_to_waitlist();
```

### 相对路径

```rust
mod front_of_house {
    pub mod hosting {
        pub fn add_to_waitlist() {}
    }
}

fn serve() {
    // 相对路径：从当前模块开始
    front_of_house::hosting::add_to_waitlist();
}

mod back_of_house {
    fn fix_order() {
        // super 指向父模块
        super::front_of_house::hosting::add_to_waitlist();
    }
}
```

### 路径选择建议

```
┌─────────────────────────────────────────────────────┐
│              路径选择指南                            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  使用绝对路径（crate::...）当：                      │
│  • 引用顶层公共 API                                  │
│  • 希望路径清晰明确                                  │
│  • 模块层级较深                                      │
│                                                     │
│  使用相对路径（super::...）当：                      │
│  • 引用兄弟模块或父模块                              │
│  • 模块层级较浅                                      │
│  • 路径很短（如 self::func）                         │
│                                                     │
│  实际项目中，推荐使用绝对路径                        │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 20.4 可见性控制

### pub 关键字

```rust
// 默认私有：模块外不可见
mod private_mod {
    fn private_func() {}          // 私有函数
    pub fn public_func() {}       // 模块内可见
}

// 公共模块
pub mod public_mod {
    pub fn public_func() {}       // 外部可见
    fn private_func() {}          // 模块内可见
}

// 结构体可见性
struct PrivateStruct {            // 私有结构体
    pub x: i32,                   // 但结构体私有，字段也无效
}

pub struct PublicStruct {         // 公共结构体
    pub x: i32,                   // 外部可访问
    y: i32,                       // 包内可访问（默认）
    pub(crate) z: i32,            // 整个 crate 内可见
}
```

### 可见性修饰符

```rust
// pub(crate)：整个 crate 内可见
pub(crate) struct CrateWide {
    // 只能在当前 crate 内访问
}

// pub(super)：父模块内可见
pub(super) struct ParentOnly {
    // 只能在父模块访问
}

// pub(in path)：指定路径内可见
pub(in crate::module) struct ModuleOnly {
    // 只能在 crate::module 内访问
}

// 示例
mod outer {
    pub(super) fn visible_to_parent() {}

    mod inner {
        // 可以访问
        super::visible_to_parent();
    }
}
```

---

## 20.5 use 关键字：引入模块

### 基本用法

```rust
mod front_of_house {
    pub mod hosting {
        pub fn add_to_waitlist() {}
        pub fn seat_at_table() {}
    }
}

// 方式 1：引入模块
use front_of_house::hosting;

fn main() {
    hosting::add_to_waitlist();
}

// 方式 2：引入具体项
use front_of_house::hosting::add_to_waitlist;

fn serve() {
    add_to_waitlist();  // 直接使用
}
```

### 使用 as 重命名

```rust
use std::fmt::Result;
use std::io::Result as IoResult;

fn function1() -> Result {
    Ok(())
}

fn function2() -> IoResult<()> {
    Ok(())
}

// 解决命名冲突
use std::fmt::Debug as FmtDebug;
use std::fmt::Display as FmtDisplay;
```

### 重新导出（pub use）

```rust
// lib.rs
mod front_of_house {
    pub mod hosting {
        pub fn add_to_waitlist() {}
    }
}

// 重新导出，让外部使用更简洁的路径
pub use crate::front_of_house::hosting;

// 外部代码可以这样用：
// use my_crate::hosting;  // 而不是 my_crate::front_of_house::hosting
```

### 使用 * 导入所有项

```rust
// 导入模块所有公共项
use std::collections::*;

fn main() {
    let v: Vec<i32> = Vec::new();      // 来自 std::collections
    let h: HashMap<i32, i32> = HashMap::new();
}

// 注意：不推荐在库代码中使用 * 导入
// 原因：
// • 难以追踪名称来源
// • 可能导致命名冲突
// • 降低代码可读性
```

---

## 20.6 模块文件分离

### 传统方式（mod.rs）

```bash
src/
├── lib.rs
├── front_of_house.rs
└── front_of_house/
    ├── mod.rs
    └── hosting.rs
```

```rust
// src/lib.rs
mod front_of_house;

// src/front_of_house.rs
pub mod hosting;

// src/front_of_house/mod.rs  (也可以放在这里)
pub mod hosting;

// src/front_of_house/hosting.rs
pub fn add_to_waitlist() {}
pub fn seat_at_table() {}
```

### Rust 2018+ 简化方式

```bash
src/
├── lib.rs
└── front_of_house/
    ├── mod.rs
    └── hosting.rs
```

```rust
// src/lib.rs
mod front_of_house;  // 自动查找 front_of_house/mod.rs 或 front_of_house.rs

// src/front_of_house/mod.rs
pub mod hosting;  // 自动查找 hosting.rs

// src/front_of_house/hosting.rs
pub fn add_to_waitlist() {}
```

### 模块文件规则

```
┌─────────────────────────────────────────────────────┐
│              模块文件查找规则                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│  声明：mod foo;                                     │
│                                                     │
│  编译器查找：                                        │
│  1. src/foo.rs                                      │
│  2. src/foo/mod.rs                                  │
│                                                     │
│  嵌套模块：mod foo { mod bar; }                     │
│                                                     │
│  编译器查找：                                        │
│  1. src/foo/bar.rs                                  │
│  2. src/foo/bar/mod.rs                              │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 20.7 完整示例：餐厅系统

### 项目结构

```bash
src/
├── lib.rs
├── front_of_house/
│   ├── mod.rs
│   ├── hosting.rs
│   └── serving.rs
├── back_of_house/
│   ├── mod.rs
│   └── cooking.rs
└── main.rs
```

### 代码实现

```rust
// src/lib.rs
pub mod front_of_house;
pub mod back_of_house;

pub fn serve_dinner() {
    front_of_house::hosting::add_to_waitlist();
    front_of_house::serving::take_order();
    back_of_house::cooking::cook_dish();
    front_of_house::serving::serve_order();
}
```

```rust
// src/front_of_house/mod.rs
pub mod hosting;
pub mod serving;
```

```rust
// src/front_of_house/hosting.rs
pub fn add_to_waitlist() {
    println!("Added to waitlist");
}

pub fn seat_at_table(table_id: usize) -> usize {
    println!("Seating at table {}", table_id);
    table_id
}
```

```rust
// src/front_of_house/serving.rs
pub fn take_order() {
    println!("Taking order");
}

pub fn serve_order() {
    println!("Serving order");
}
```

```rust
// src/back_of_house/mod.rs
pub mod cooking;
```

```rust
// src/back_of_house/cooking.rs
pub fn cook_dish() {
    println!("Cooking dish");
}

pub struct Chef {
    pub name: String,
}

impl Chef {
    pub fn new(name: String) -> Self {
        Chef { name }
    }

    pub fn cook(&self) {
        println!("{} is cooking", self.name);
    }
}
```

```rust
// src/main.rs
use restaurant::front_of_house::hosting;
use restaurant::back_of_house::Chef;

fn main() {
    hosting::add_to_waitlist();
    hosting::seat_at_table(5);

    let chef = Chef::new(String::from("Gordon"));
    chef.cook();

    restaurant::serve_dinner();
}
```

---

## 20.8 Cargo.toml 配置

### 基本配置

```toml
[package]
name = "my_package"
version = "0.1.0"
edition = "2021"
authors = ["Your Name <you@example.com>"]
description = "A brief description"
license = "MIT"
repository = "https://github.com/user/repo"
keywords = ["keyword1", "keyword2"]
categories = ["development-tools"]
```

### 定义多个二进制

```toml
[[bin]]
name = "my_tool"
path = "src/bin/my_tool.rs"

[[bin]]
name = "my_server"
path = "src/bin/my_server.rs"
```

### 库配置

```toml
[lib]
name = "my_lib"
path = "src/lib.rs"
crate-type = ["lib", "rlib", "cdylib"]
```

---

## 20.9 常见错误

### 错误 1：模块文件找不到

```
error[E0583]: file not found for module `xyz`
 --> src/lib.rs:1:1
  |
1 | mod xyz;
  | ^^^^^^^^

原因：缺少 src/xyz.rs 或 src/xyz/mod.rs
解决：创建对应的模块文件
```

### 错误 2：私有模块无法访问

```rust
mod private {
    pub fn func() {}
}

fn main() {
    // private::func();  // ❌ 错误：模块是私有的
}

// 修复
pub mod public {
    pub fn func() {}
}

fn main() {
    public::func();  // ✅
}
```

### 错误 3：路径错误

```rust
// ❌ 错误
// use non_existent::module;

// ✅ 正确
use crate::existent::module;
```

### 错误 4：循环依赖

```rust
// ❌ 错误
mod a {
    use crate::b;  // a 使用 b
}

mod b {
    use crate::a;  // b 使用 a，循环依赖
}

// 解决：重构代码，避免循环依赖
```

---

## 20.10 练习

### 练习 1：基本模块

创建一个库项目，包含以下模块：
- `math`：包含 `add`, `subtract`, `multiply`, `divide` 函数
- `string`：包含 `reverse`, `uppercase`, `lowercase` 函数

### 练习 2：模块文件分离

将练习 1 的代码分离到多个文件中：
```
src/
├── lib.rs
├── math.rs
└── string.rs
```

### 练习 3：重新导出

使用 `pub use`重新导出`math::add`和`math::subtract`，使得外部可以直接使用`my_crate::add`。

---

## 20.11 小结

本章我们学习了：

- ✅ Crate 和 Package 的概念
- ✅ 模块的定义和嵌套
- ✅ 绝对路径和相对路径
- ✅ 可见性控制（pub 及修饰符）
- ✅ use 关键字和重新导出
- ✅ 模块文件分离
- ✅ 常见错误和解决方案

### 模块系统速查表

| 语法 | 说明 |
|------|------|
| `mod foo;` | 声明模块 |
| `pub mod foo;` | 声明公共模块 |
| `use foo::bar;` | 引入模块 |
| `pub use foo::bar;` | 重新导出 |
| `crate::` | crate 根 |
| `super::` | 父模块 |
| `self::` | 当前模块 |

---

## 下一章

[第 21 章：Cargo 与 Crates](21-Cargo 与 Crates.md)
