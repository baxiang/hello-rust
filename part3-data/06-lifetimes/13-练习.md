## 17.13 练习

### 练习 1：基本生命周期标注

为以下结构体添加正确的生命周期标注：
```rust
struct Reference {
    data: &i32,
}
```

### 练习 2：返回较短字符串

编写函数，返回两个字符串中较短的一个：
```rust
fn shortest(x: &str, y: &str) -> &str {
    // 实现
}
```

### 练习 3：修复错误

找出并修复以下代码的错误：
```rust
fn get_first(s: &str) -> &str {
    &s[0..1]
}

struct Holder {
    value: &str,
}
```

---

---

## 下一步

[小结](../14-小结.md)