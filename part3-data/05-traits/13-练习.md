## 16.13 练习

### 练习 1：Area Trait

定义一个 `Area` trait，为不同形状计算面积：
- Circle（圆）
- Rectangle（矩形）
- Triangle（三角形）

### 练习 2：Serialize Trait

实现一个 `Serialize` trait：
```rust
trait Serialize {
    fn to_json(&self) -> String;
}
```

为 User 结构体实现该 trait。

### 练习 3：Compare Trait

创建 `Compare` trait，实现：
- `max(a, b)` - 返回较大值
- `min(a, b)` - 返回较小值

---

---

## 下一步

[小结](../14-小结.md)