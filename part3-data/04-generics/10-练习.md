## 15.10 练习

### 练习 1：泛型交换函数

编写一个泛型函数，交换两个变量的值：
```rust
fn swap<T>(a: &mut T, b: &mut T) {
    // 实现
}
```

### 练习 2：泛型 LinkedList

实现一个泛型链表：
```rust
struct LinkedList<T> {
    // 定义
}

impl<T> LinkedList<T> {
    fn new() -> Self { }
    fn push_front(&mut self, value: T) { }
    fn pop_front(&mut self) -> Option<T> { }
}
```

### 练习 3：泛型过滤器

实现一个泛型过滤函数：
```rust
fn filter<T, F>(vec: Vec<T>, predicate: F) -> Vec<T>
where
    F: Fn(&T) -> bool,
{
    // 保留满足条件的元素
}
```

---

---

## 下一步

[小结](../11-小结.md)