## 10.14 小结

### 结构体要点

| 概念 | 说明 |
|------|------|
| 命名字段 | `struct Person { name: String }` |
| 元组字段 | `struct Color(i32, i32, i32)` |
| 单元结构体 | `struct Marker;` |
| 方法 | `fn method(&self)` |
| 关联函数 | `fn new() -> Self` |
| 派生 Trait | `#[derive(Debug, Clone)]` |

### self 的三种形式

| 形式 | 用途 | 示例 |
|------|------|------|
| `&self` | 只读访问 | `fn area(&self)` |
| `&mut self` | 修改数据 | `fn push(&mut self)` |
| `self` | 消耗实例 | `fn into_inner(self)` |

### 最佳实践

1. **使用有意义的字段名**，提高代码可读性
2. **实现 `new` 构造函数**，方便创建实例
3. **派生常用 Trait**（Debug, Clone, PartialEq）
4. **区分&self 和&mut self**，遵循借用规则
5. **使用单元结构体作为标记类型**

---

## 下一章

[第 11 章：枚举与模式匹配](11-枚举与模式匹配.md)

---

## 下一章

[返回目录](../../README.md)